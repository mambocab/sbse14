language: python
python:
  - "2.7"
install:
  - pip install -r reqirements.txt
  - pip install coveralls
before_script:  flake8 witschey/
script:         nosetests --with-doctest --with-coverage --cover-package witschey witschey
after_script:   coveralls
